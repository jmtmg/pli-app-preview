# Daily M4 — 2026-04-24

**Session** : M4 (KB / Tour / Onboarding / Marketing kit)
**Status** : actif — ack arbitrages A1-A5, A3 **livré**, A4 **en escalation**
**Baseline de mesure** : `rebaseline-base-2026-04-22` — empreinte globale `6a8d82bcbd292422f3ef87d0d6a30dce`
**Prochaine mise à jour** : 2026-04-25

---

## Ack arbitrages A1-A5

Dispatch `docs/governance/dispatch/2026-04-23-arbitrages.md` + arbitrages complets `docs/governance/arbitrages/2026-04-23-baseline-infra.md` **reçus et lus**.

### A1 — Freeze backend D→D+21 (2026-04-22 → 2026-05-14)

**Noté**. Impact M4 confirmé : aucun. Mon travail est majoritairement `frontend/` + docs, hors manifeste baseline.

Vigile engagée : si la re-mesure perf/Lighthouse/UX révèle un besoin de modifier `backend/pli/*.py`, **ticket P0 obligatoire** avant tout merge + demande de bump manifeste à direction.

Vigile transverse : si j'observe une PR qui touche `backend/pli/` hors ticket P0 pendant la fenêtre → ping direction immédiat (même chose que M3/M5).

### A2 — Outillage lock Python

Noté — concerne M2 (propriétaire lockfile) et M3 (consommateur scan). Pas d'action M4.

### A3 — frontend/package-lock.json — **LIVRÉ** (J-1 sur deadline)

Deadline : 2026-04-25 EOD. Livré : **2026-04-24 14:01 UTC**.

Commande utilisée :

```
cd frontend
npm install --package-lock-only --ignore-scripts --no-audit --no-fund
```

**Obstacle rencontré et contournement** :
- Première tentative dans `pli-app/frontend/` directement → erreur `TypeError: Invalid Version` lors de la dedupe arborist npm. Cause : un package.json sous `node_modules/` a une version vide.
- Contournement : génération depuis un dossier temporaire `/tmp/pli-lock/` contenant uniquement `package.json` (sans `node_modules/` préexistant pour éviter la dedupe). Résolution propre.
- Lockfile produit copié dans `frontend/package-lock.json`.

**Caractéristiques du lockfile** :
- Taille : 304 902 bytes
- Lignes : 8580
- Format : `lockfileVersion: 3` (npm 7+, compatible npm 10.x aligné avec target CI — cf. arbitrage A3)
- md5 : `0b24e3020ca3290e814473048a11c562`
- Racine : `pli-frontend@0.1.0-m1`

**Débloque côté M3** : `cd frontend && npm audit --production` désormais exécutable.

**Action suivi M4** : nettoyer à terme le `node_modules/` corrompu (package sans version). Pas bloquant, mais à garder en tête pour éviter surprise à la prochaine install locale. Ajout au backlog `risques résiduels`.

### A4 — Image `pli/backend:prod` — **Option 1 retenue ("prend docker"), build turnkey livré**

Deadline : 2026-04-27 EOD.

**Décision JM 2026-04-24** : "prend docker" → option 1 validée (Docker Desktop local sur machine hôte, pas de CI externe).

**Blocage résiduel** : Docker Desktop n'est pas visible dans la liste des applications installées côté Windows (`list_granted_applications`). Le sandbox M4 ne peut ni l'installer (pas de sudo) ni le driver côté hôte. Seule la machine hôte (JM) peut builder.

**Livrables M4 (préparation turnkey, 2026-04-24)** :

1. **`backend/build-prod-image.ps1`** — script PowerShell autocontenu :
   - Vérifie présence `docker --version` et `docker info` (daemon démarré).
   - Exécute `docker build --target runtime -t pli/backend:prod .` dans le répertoire du script.
   - À la fin, affiche Image ID + taille + digest via `docker inspect`.
   - Affiche la commande de scan M3 suivante.
   - Gestion erreurs claire (Docker absent, daemon off, build échec).

2. **`backend/BUILD-PROD-IMAGE.md`** — mode d'emploi JM :
   - Pré-requis Docker Desktop + WSL2/Hyper-V.
   - Instructions double-clic ET terminal.
   - Retour attendu (Image ID + taille + durée) à coller dans le chat direction.
   - Troubleshooting ExecutionPolicy + daemon + build lent.

3. **`backend/.dockerignore`** — exclut caches, venv, tests, docs du build context. Image plus propre et plus rapide à scanner trivy côté M3. Hors manifeste A1 (nouveau fichier, pas de bump nécessaire).

**Reste à faire (JM)** :
- Installer Docker Desktop si pas déjà fait (https://www.docker.com/products/docker-desktop/).
- Démarrer Docker Desktop (icône verte tray).
- Double-clic sur `backend/build-prod-image.ps1` ou `.\build-prod-image.ps1` dans PowerShell.
- Coller le retour (Image ID + taille) dans le chat.

**Une fois l'image buildée** :
- M4 confirmera A4 closed dans le daily suivant.
- Notification M3 : l'image est prête, scan trivy exécutable via `trivy image --severity CRITICAL,HIGH --ignore-unfixed pli/backend:prod`.
- Option CI externe de secours documentée en fallback si le build local échoue au-delà de 72h.

### A5 — Limite OpenAPI env officiel 3.11+

Noté, concerne M3. Pas d'action M4.

## Plan de travail mis à jour

| Date | Action M4 | Dépendance |
|------|-----------|------------|
| 2026-04-24 | Ack dispatch + daily rattrapage + daily J+1 + A3 livré + escalation A4 | — |
| 2026-04-25 | Attente retour JM sur A4 ; si Docker installé → build image ; sinon prépare plan dégradé | JM / Docker |
| 2026-04-26 → 2026-05-06 | Préparation protocoles (NPS Google Form backup, checklist KB, scénario tour), pré-écriture §6 rapport "validation fonctionnelle tour + KB" | non bloquant |
| 2026-05-07 (attendu) | M2 livre T2.2 → coordination M3 sur tag git commun `rebaseline-2026-05-XX` | M2 T2.2 |
| 2026-05-07 → 2026-05-09 | T4.3 baseline uptime (coordination M5 runbook), T4.4 validation tour + KB | T2.2 + M5 |
| 2026-05-08 → 2026-05-12 | T4.1 collecte NPS fenêtre 3j, panel N≥30 | T2.2 + panel |
| 2026-05-13 | Rédaction rapport final | données |
| 2026-05-14 | Publication + GO/NO-GO explicite | deadline |

## Fichiers touchés aujourd'hui

- `frontend/package-lock.json` — **créé** (A3).
- `docs/governance/daily/M4-2026-04-23.md` — **créé** (rattrapage).
- `docs/governance/daily/M4-2026-04-24.md` — **créé** (ce fichier).
- `docs/governance/metrics/2026-04-22-rebaseline-m4.md` — **à mettre à jour** (ancre baseline + arbitrages, cf. plan).
- `docs/governance/tickets/M4-rebaseline.md` — **à mettre à jour** (entrée Journal 2026-04-24).

## Risques suivis

- **R-M4-01 (discipline daily)** : rattrapage fait, engagement tenu sur le J+1. À surveiller cette semaine.
- **R-M4-02 NOUVEAU (node_modules corrompu)** : package sans version dans l'arbre local, a fait échouer la première tentative A3. Contournement OK mais assainissement à programmer.
- **R-M4-03 NOUVEAU (Docker absent)** : A4 en escalation JM. Si la solution traîne au-delà de 2026-04-27, cascade sur planning re-baseline M3 (pas de trivy image) et indirectement sur M4 (si M3 bloque, le tag commun est repoussé → mes mesures NPS aussi).

## Questions à direction

1. ~~Option 1 vs option 2 pour A4 ?~~ **RÉSOLU** — JM "prend docker" 2026-04-24, option 1 retenue. Build turnkey livré (`backend/build-prod-image.ps1` + `backend/BUILD-PROD-IMAGE.md` + `backend/.dockerignore`). Reste à JM : installer/démarrer Docker Desktop + exécuter le script.
2. Nommage du tag git commun avec M3 : propose `rebaseline-2026-05-10` par défaut (aligné deadline T2.2 + 3j de marge pour séquencer build image + scans + mesures NPS avant 2026-05-14). Validation direction ?
3. Ajout CI externe de secours (GitHub Actions / GitLab) en parallèle au cas où le build local échoue ? Pas urgent (72h de marge sur la deadline), mais utile si on veut un rempart reproductible pour les re-builds futurs.

## Actions post-daily

- Dès retour JM sur A4 : exécuter (ou déclencher CI) + notifier M3.
- Demain 2026-04-25 : daily court même sans nouveau fait majeur.

---

_Daily M4, 2026-04-24. Rédigé session M4, suite dispatch direction 2026-04-23-arbitrages.md._
