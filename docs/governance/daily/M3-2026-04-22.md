# Daily M3 — 2026-04-22

**Session** : M3 (Bêta / OCR / sécu)
**Status** : actif — préparation pendant blocage M2
**Prochaine mise à jour** : 2026-04-23

---

## Ack

- Ordre de mission `2026-04-22-ordre-mission.md` lu et appliqué.
- Ticket `tickets/M3-rebaseline.md` récupéré.
- Bloqué par M2 T2.2 (wire-up `PLI_ENABLE_M2`, deadline 2026-05-07).
- Deadline M3 : 2026-05-14 — 22 jours de runway après déblocage estimé.
- Aucun nouveau scope engagé (règle convergence §4 de l'ordre de mission respectée).

## Constat sur le M3-CLOSEOUT signé la veille

Les chiffres que j'ai revendiqués (Lighthouse 0.93 / 0.98, 0 Critical / 0 High) ont été mesurés hors binaire intégré. En l'état, ils ne valent pas. Le document `M3-CLOSEOUT.md` reste sur disque à titre de trace, mais je considère que la décision GO pour M4 doit être **suspendue** jusqu'à re-baseline. Je ne retouche pas le document (traçabilité audit) mais j'ajoute une note en tête dans une mise à jour dédiée — proposition soumise à direction pour arbitrage.

## Fait aujourd'hui (prep work, pas de mesure officielle)

- Config Lighthouse CI dédiée au binaire intégré avec parcours Billing portal : `e2e/perf/lighthouserc.integrated.cjs`. Assertions 0.90 perf / 0.95 best practices / 0.95 a11y. Core Web Vitals LCP 2500 / CLS 0.1 / INP 200 / TBT 300 conservés.
- Runner shell `e2e/perf/run-lighthouse-integrated.sh` avec garde-fou explicite : refuse de tourner si `PLI_ENABLE_M2 != 1` ou si `/api/billing/ping` ne répond pas. Objectif : impossible de reproduire l'erreur pointée par l'audit.
- Scripts scan vulns `scripts/rebaseline/run-vuln-scans.sh` + agrégateur Python `summarize-scans.py`. pip-audit sur requirements.lock, npm audit --production, trivy image --severity CRITICAL,HIGH --ignore-unfixed. Exit 1 si Critical+High > 0.
- Test E2E OCR sur chemin intégré `e2e/ocr/integrated.spec.ts` (Playwright) : upload PDF scanné → enqueue → poll status 10 s → verif text + FTS5. Skip automatique si `PLI_ENABLE_M2` absent.
- Tâches ajoutées au tracker M3 (#15 à #19).

## Blocage confirmé

Vérifié manuellement `backend/pli/main.py` : seuls les 6 routers Sprint 1 sont montés (auth / accounts / conversations / messages / contacts / search). Pas de `billing`, pas de `gdpr`, pas d'`ocr`. Le constat de l'audit 2026-04-22 est reproduit, le diagnostic est bon.

## Dépendances amont

- **M2 T2.1** — résolution `ModuleNotFoundError: pli.auth` (deadline 2026-04-30). Sans ça, `pytest --collect-only` ne passera pas et je ne peux pas lancer mes E2E OCR même avec M2 wire-up.
- **M2 T2.2** — wire-up routers derrière `PLI_ENABLE_M2` (deadline 2026-05-07). Sans ça, impossible de re-baseliner.
- **M2** me doit également un ping quand `/api/billing/ping` est up — j'ai câblé le garde-fou dessus dans `run-lighthouse-integrated.sh`.

## Prévu demain (2026-04-23)

- Préparer la trame du rapport de synthèse `docs/governance/metrics/2026-04-22-rebaseline-m3.md` (tableau avant / après vide, sections à remplir).
- Relire les 27 contrôles de `security/threat-model-checklist.md` et vérifier lesquels supposaient un binaire intégré — candidats à re-tester en même temps que T3.2.
- Préparer le jeu de fixtures OCR (`backend/tests/fixtures/ocr/invoice_scanned_3pages.pdf`) : référencé par le nouveau E2E mais pas encore committé.
- Pas de mesure officielle tant que M2 n'a pas confirmé T2.2.

## Risques suivis

- **R-M3-01** : si M2 glisse T2.2 au-delà du 2026-05-10, ma fenêtre de re-baseline se réduit à 4 jours. Envisager de demander à direction un délai supplémentaire si glissement confirmé.
- **R-M3-02** : si les chiffres re-baselinés sont réellement inférieurs aux cibles (par exemple 0.87 perf après wire-up), il faudra ouvrir un ticket perf tuning. Anticiper : profiler le binaire `PLI_ENABLE_M2=1` dès que disponible pour identifier les coûts additionnels (imports, middlewares).
- **R-M3-03** : `trivy image --ignore-unfixed` peut masquer des CVE qu'on n'aurait pas eues avec une version récente de la dépendance. À discuter avec direction si on veut un second run sans `--ignore-unfixed` pour avoir la photo complète, quitte à documenter les acceptations.

## Questions à direction

1. OK pour suspendre la décision GO M4 jusqu'à re-baseline terminée ?
2. Est-ce que je dois coordonner le jour de mesure avec M4 (pour qu'on mesure sur le même build, même seed DB) ou chacun de son côté ?
3. Si une régression > 5 pts apparaît sur un parcours, préférence direction : je documente et clos M3 (avec ticket perf tuning en file) ou je bloque la clôture M3 tant que le perf tuning n'est pas fait ?

## Réponses direction (reçues le 2026-04-22)

1. **Suspension GO M4 : OUI formel.** GO conditionné à re-baseline M3+M4 sur binaire intégré. Ne remet pas en cause le travail M4, juste son cadre de mesure. M5 sera prévenu via son daily.
2. **Même build figé.** Après T2.2 validé, M3 et M4 se coordonnent pour poser un tag git commun (ex. `rebaseline-2026-05-10`) et mesurent depuis ce tag. Évite l'écart « M3 à X, M4 à Y, non comparables ».
3. **Seuil gradué** :
   - ≤ 10 pts perf ET ≤ 5 pts best practices : clôture M3 avec ticket perf tuning en file, Launch non bloqué.
   - > 10 pts perf OU > 5 pts best practices : clôture bloquée, perf tuning obligatoire avant feu vert.
   - 0 Critical / 0 High reste non négociable (aucun gradient sécu).

## Actions post-arbitrages (réalisées le 2026-04-22)

- En-tête de suspension ajouté dans `M3-CLOSEOUT.md` pointant vers le ticket re-baseline.
- Trame rapport de synthèse T3.4 : `docs/governance/metrics/2026-04-22-rebaseline-m3.md` (tableau avant/après à 5 parcours, gate sécu non négociable, gate perf gradué).
- Checklist coordination M3/M4 : `docs/governance/metrics/REBASELINE-CHECKLIST.md` (protocole séquentiel tag git + seed partagée + signature croisée).
- Runner Lighthouse et runner scan vulns mis à jour : garde-fou `git describe --exact-match --tags HEAD` doit renvoyer `rebaseline-*`, override explicite `PLI_REBASELINE_ALLOW_UNTAGGED=1` pour dry-run local.
- Ticket `M3-rebaseline.md` Journal complété avec les arbitrages et la liste d'actions.

---

_Rédigé par session M3, 2026-04-22._
