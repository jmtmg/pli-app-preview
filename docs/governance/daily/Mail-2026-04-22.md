# Daily direction Mail — 2026-04-22

## Synthèse

Journée d'arbitrage post-dispatch ordre de mission. 5 sessions M1-M5 ont
livré leurs deliverables. Deux itérations d'arbitrage : (1) validation
Sprint 1 + déblocage M2, (2) closeout T1.6 M1 + fix P0 M2.

## Décisions prises

### D1 — ADR 0008 Integration freeze
Signé direction, deadline 2026-08-26 non-négociable. Sprint 11 §7/§8 M5
enrichi (pré-requis re-baseline).

### D2 — Kill-switch M2
ADR 0007 feature-flag-m2 accepté. `PLI_ENABLE_M2=1` activé en mode (b).
Note ops : ne **jamais** mettre `PLI_ENABLE_M2=''` (chaîne vide →
ValidationError pydantic au boot). Unset si off.

### D3 — Règle hygiène OneDrive (§8 ordre de mission)
Pattern systémique de truncation détecté (conftest.py 49/71, adapters/local.py
191/227, LocalCryptoAdapter partiel). Règle : préférer bash heredoc pour
>100 lignes, vérifier post-write (`wc -l` + `py_compile` + `pytest --collect-only`).

### D4 — Fix P0 `current_local_plan`
M1 a détecté pendant T1.6 que mode (b) faisait 50/50 KO par ImportError.
M2 a appliqué Fix C (plan="free") + extra `[project.optional-dependencies].m2`.
Validation croisée M1/M2 : 50/50 pytest dans les 2 modes.

### D5 — Baseline coordonnée `rebaseline-base-2026-04-22`
Workspace OneDrive non git-init, donc **manifeste md5 logique** substitué
au tag git classique.
Fichier : `docs/governance/tags/rebaseline-base-2026-04-22.md`.
Empreinte globale : `6a8d82bcbd292422f3ef87d0d6a30dce`.
Coverage : 72 `pli/*.py` + 24 `tests/*.py` + `pyproject.toml` + 8 ADRs = 105 hashes.
Tag git différé (sprint dédié, non-bloquant).

## État à J0+0

| Session | Livrable J-1 | Livrable J-0 | État |
|---------|--------------|--------------|------|
| M1 | Sprint 1 50/50 pytest | T1.6 CLOSED (50/50 deux modes), P0 détecté | ✅ |
| M2 | ADR 0007 + wire-up M2 | Fix C + pyproject extra m2 + accountability conftest | ✅ |
| M3 | REBASELINE-CHECKLIST | (attente baseline) | ⏳ WAITING |
| M4 | Méthodologie NPS/J30/uptime | (attente baseline) | ⏳ WAITING |
| M5 | ADR 0008 + Sprint 11 §7/§8 | Runbook §13 v1.1 | ✅ |

## Actions humaines en cours (JM)

- M1 T1.2 : OAuth Gmail credentials (console Google Cloud, ~15 min)
- M1 T1.3 : 4 screenshots runthrough E2E (~10 min)

Non-bloquant pour D5. Baseline posée indépendamment.

## Prochaines étapes

1. **Aujourd'hui** : notifier M3 + M4 + M5 empreinte baseline `6a8d82bc…`.
2. **J+1 (2026-04-23)** : M1 poste closeout daily, M2 poste T2.3 checkpoint 38/42.
3. **J+7 (2026-04-29)** : sync hebdo — premier contrôle convergence M3/M4 sur baseline.
4. **J+21 (2026-05-14)** : deadline rebaseline M3 (CLOSEOUT re-signé sur binaire intégré) + M4 (NPS/J30/uptime mesurés sur baseline figée).

## Risques actifs

- **R-HYG-01** : drift OneDrive. Mitigé par §8 hygiène + manifeste md5 (détecte tout changement silencieux).
- **R-GIT-01** : absence git repo (workspace OneDrive pur). Mitigé par manifeste md5 logique. Migration vers vrai tag git = tâche non-bloquante sprint dédié.
- **R-M2-02** (downgraded) : hygiène conftest.py. Résolu par reconstruction M1 + accountability M2. Surveiller re-occurrence.
