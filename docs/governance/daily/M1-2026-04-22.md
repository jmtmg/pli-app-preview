# Daily M1 — 2026-04-22

**Session** : M1 (mail conversationnel)
**Format** : log daily — ordre de mission 2026-04-22 §4
**Statut sprint** : Sprint 1 closeout — exécution ticket
`M1-sprint1-closeout.md`

---

## Réalisé aujourd'hui

- **T1.1** ✅ Validation patches Mail (`accounts.py`, `messages.py`,
  `contacts.py` + 30 tests). Verdict accepté en l'état. Inexactitude
  ticket flagguée : code requiert Python 3.11+ (pas 3.10) — cohérent
  avec `pyproject.toml` mais le ticket annonçait 3.10 par erreur.
- **T1.4** ✅ Refresh `docs/09-API-Contract.md` — section §22 Sprint 1
  effectif ajoutée (≈ 220 lignes) avec DTOs réels, écarts spec v1
  documentés, table de correspondance BE-FE.
- **T1.5** ✅ Création `docs/sprint-planning/sprint-2-backlog.md` —
  parking explicite US-1.2 (Microsoft OAuth) + US-1.5 (sync Graph) +
  4 items de dette identifiés (D-2.1 à D-2.4).
- **T1.2** 🟡 partiel — `.env.example` corrigé (`PLI_GOOGLE_*` →
  `PLI_GMAIL_*`, bug bloquant pour qui aurait copié), runbook
  `docs/runbooks/oauth-gmail-setup.md` créé (170 lignes). Validation
  E2E reste à faire par opérateur humain.
- **T1.3** 🟡 partiel — kit capture créé dans
  `docs/governance/demo/m1-e2e-2026-04-22/` (README scénario + script
  `preflight.sh` exécutable). Capture des 4 screenshots reste à faire.
- **T1.6** 🔴 RÉGRESSION DÉTECTÉE — voir section dédiée ci-dessous.
- **T1.7** ⛔ BLOQUÉ par T1.6. Tag `rebaseline-base-2026-04-22` non
  posé : on ne pose pas de baseline sur un binaire cassé. À débloquer
  une fois le hotfix M2 mergé.

## T1.6 non-régression — verdict 🔴 RÉGRESSION

Hotfix conftest préalable : `backend/tests/conftest.py` était tronqué
sur disque (49 lignes, coupé en plein commentaire), aucun `client`
fixture défini → `fixture 'client' not found` sur les 50 tests Sprint 1.
Reconstruction (71 lignes) : queue de `isolated_settings` (cache_clear
+ yield + cleanup) + `client` (TestClient FastAPI sur `pli.main:app`,
`follow_redirects=False`).

### Mode (a) — `PLI_ENABLE_M2` unset (défaut Sprint 1)

✅ Démarrage propre. Log `pli_m2_routers_disabled` confirmé.
✅ `app.routes` = 21 paths (6 routers Sprint 1 + endpoints FastAPI
built-ins `/`, `/health`, `/docs`, `/redoc`, `/openapi.json`).
Aucun endpoint M2 exposé (`/auth/login`, `/auth/signup`, `/billing/*`,
`/licenses/*` absents).
✅ `pytest tests/test_accounts.py tests/test_messages.py
tests/test_contacts.py tests/test_conversations.py tests/test_auth_oauth.py`
→ **50 passed in 3.24s**.

### Mode (b) — `PLI_ENABLE_M2=1`

✅ Démarrage propre côté import après installation des deps M2 (cf.
section "Risques" ci-dessous). Log `pli_m2_routers_enabled` confirmé.
✅ `app.routes` = 35 paths (+14 vs mode a) : `/auth/login`,
`/auth/signup`, `/auth/refresh`, `/auth/verify`, `/auth/logout`,
`/auth/password-reset/{request,confirm}`, `/billing/billing/*`,
`/licenses/licenses/*`. Aucune collision avec les routes Sprint 1
(`/auth/{provider}/start` ≠ `/auth/login`).

🔴 `pytest` mêmes suites Sprint 1 → **50 failed in 8.58s**.

**Cause racine** : `TenantContextMiddleware` (ajouté global par M2) appelle
sur chaque requête le `local_principal_resolver` (`pli/adapters/local.py:137`)
qui fait `from ..billing.licenses import current_local_plan`. Mais
`pli/billing/licenses.py` (65 lignes) n'exporte que
`issue_license_for_subscription` et `get_active_license` — `current_local_plan`
n'existe nulle part dans le repo.

→ **Erreur unique propagée** :
`401 auth_failed:cannot import name 'current_local_plan' from
'pli.billing.licenses'`. 100 % des endpoints Sprint 1 KO en mode (b).

Régression bloquante : la baseline rebaseline-base-2026-04-22 n'est pas
postable. Ticket ouvert : `docs/governance/tickets/M1-regression-m2.md`.

### Note dépendances M2 manquantes

Pour faire démarrer l'app en mode (b) en sandbox propre, il a fallu
installer manuellement : `pyjwt`, `stripe`, `python-ulid`, `argon2-cffi`,
`asyncpg`, `jinja2`, `tomli_w`. Ces packages sont **utilisés** par M2 et
M3 mais ne figurent pas dans `backend/pyproject.toml` (qui liste
seulement les deps Sprint 1 + `pysqlcipher3`). À ajouter par M2 dans
son prochain commit (sinon `pip install -e .` ne suffit pas pour
booter avec `PLI_ENABLE_M2=1`).

## Bloqué / WAITING_HUMAN

- **T1.2** : nécessite création projet Google Cloud Console + écran de
  consentement + test user (intrinsèquement humain, runbook prêt).
- **T1.3** : dépend T1.2 + capture navigateur (humaine).

## En cours demain

- Daily du 2026-04-23 (premier daily M1 régulier post-closeout)
- Si opérateur a exécuté T1.2 : lecture du retour, validation des
  screenshots, marquage "closed" effectif des deux tâches WAITING_HUMAN.

## Risques / alertes direction

1. **Risk Python 3.10 dans la doc** : si une autre session (M2/M3/M4)
   ré-utilise des extraits de la doc Sprint 1 mentionnant "tests
   passants sur sandbox 3.10", elle pourrait perdre du temps à débugger
   un faux problème. Recommandation à direction : préciser "Python 3.11+
   requis" dans les futurs ordres de mission.
2. **Risk dépendance M2** : US-1.2 (Microsoft OAuth) Sprint 2 dépend
   peut-être de helpers `pli.auth` actuellement cassés (cf. ordre de
   mission D2). À clarifier au sync 2026-04-27.
3. **Pas de risque démo** : tous les patches Sprint 1 sont validés
   code-side, le binaire est déployable dès que l'opérateur a connecté
   un Gmail réel — **sous condition** : ne pas déployer avec
   `PLI_ENABLE_M2=1` tant que le hotfix `current_local_plan` n'est pas
   mergé (cf. T1.6).
4. **🔴 P0 régression M2 → Sprint 1** : avec `PLI_ENABLE_M2=1`, 100 %
   des endpoints Sprint 1 retournent `401 auth_failed`. Cause :
   `pli.adapters.local.local_principal_resolver` importe `current_local_plan`
   d'un module qui ne l'exporte pas. Hotfix attendu de M2 (≤ 30 min de
   travail). Bloque le tag `rebaseline-base-2026-04-22` (T1.7) et donc
   la re-baseline coordonnée M3/M4. Ticket dédié :
   `docs/governance/tickets/M1-regression-m2.md`.
5. **conftest.py tronqué** : le fichier sur disque ne contenait que la
   moitié supérieure de `isolated_settings` et aucun `client`. Hypothèse :
   patch atomique manqué lors d'un commit antérieur (peut-être pendant
   les 30 tests Mail). Reconstruit ce jour. À surveiller : les autres
   sessions ont-elles des fichiers tronqués similaires ?

## Métriques

- 3/5 tâches DONE en autonomie (T1.1, T1.4, T1.5)
- 2/5 tâches WAITING_HUMAN (T1.2, T1.3)
- 0/2 tâches additionnelles complètes : T1.6 BLOQUÉ régression, T1.7 BLOQUÉ par T1.6
- 1 hotfix conftest.py (réparation file truncation)
- 1 régression P0 détectée et tracée (M1-regression-m2)
- 0 commit dans `main` (règle ordre de mission §2 D3 respectée)

---

_Format aligné sur `docs/governance/daily/M3-2026-04-22.md` et
`docs/governance/daily/M4-2026-04-22.md`._
