# Daily M2 — 2026-04-23

**Session** : M2 (auth PLI + billing Stripe + licensing + tenancy)
**Format** : log daily — ordre de mission 2026-04-22 §4
**Statut sprint** : hotfix P0 ticket `tickets/M1-regression-m2.md`

---

## Ack

- Ticket `tickets/M1-regression-m2.md` reçu de M1 (P0, deadline fin de
  journée 2026-04-23, cohérent avec deadline T1.7 côté M1).
- Arbitrage direction : **Fix C** (quick-win) retenu. Plan codé en dur à
  `"free"` dans `pli/adapters/local.py`, TODO Sprint 2/3 pour brancher la
  licence locale signée Ed25519. Pas d'introduction de couplage
  `billing → licensing` avant le refactor.
- Scope complémentaire obligatoire : **ajouter les 7 deps M2 manquantes**
  dans `backend/pyproject.toml`, sinon le tag rebaseline T1.7 est inutile.
- **Correction diagnostic** à porter sur le journal/daily 2026-04-22 :
  les 99 erreurs `fixture 'client' not found` n'étaient pas un problème
  `pytest-asyncio`/pinning — `tests/conftest.py` était tronqué à 49 lignes,
  reconstruit à 71 lignes par M1 (T1.6). Ne pas renvoyer la balle à M5.

## Réalisé aujourd'hui

### Fix C — `local_principal_resolver`

- `pli/adapters/local.py::local_principal_resolver` nettoyé :
  - Import cassé `from ..billing.licenses import current_local_plan`
    retiré (symbole inexistant, cf. §Cause racine du ticket).
  - Retour `Principal(tenant_id=LOCAL_TENANT_ID,
    user_id=LOCAL_TENANT_ID, plan="free", email=None)`.
  - Docstring mise à jour : pointe vers le TODO Sprint 2/3 (licence
    locale Ed25519 via `pli.licensing.local_key.load_license().plan`),
    justification « pas de couplage billing→licensing avant refactor
    Sprint 2/3 », référence ADR 0007 §Rollout.

### Deps M2 — extra `[m2]` dans `pyproject.toml`

- Ajout de `[project.optional-dependencies].m2` :
  ```toml
  m2 = [
    "pyjwt>=2.9.0",
    "argon2-cffi>=23.1.0",
    "stripe>=11.0.0",
    "asyncpg>=0.30.0",
    "jinja2>=3.1.4",
    "python-ulid>=3.0.0",
    "tomli_w>=1.0.0",
  ]
  ```
- Commentaire explicite : `pip install -e .` reste nu (Sprint 1 / M1
  mode local). Activation CI/staging par `pip install -e '.[m2]'` ou
  `'.[m2,dev]'`. Critère d'acceptation #3 du ticket respecté : **pas
  de dépendance Postgres (`asyncpg`) au boot mode local**.

### Correctifs connexes

- `pli/adapters/local.py` : fallback `tomllib`/`tomli` pour Python 3.10
  sandbox (`try: import tomllib / except ModuleNotFoundError: import
  tomli as tomllib`). Cohérent avec `pyproject.toml`
  `requires-python = ">=3.11"` (CI) ; ne crée pas de nouvelle
  dépendance en 3.11+.
- Restauration de `LocalCryptoAdapter` en queue du fichier
  `pli/adapters/local.py` après une troncature OneDrive transitoire
  pendant l'édition. Reconstruction via bash (script Python idempotent :
  refuse d'écrire si la classe est déjà présente).

### Correction Journal + daily 2026-04-22

- `docs/governance/tickets/M2-unblock-auth.md` §Preuves pytest : bloc
  « 99 errors » remplacé par la vraie cause (conftest.py tronqué, réparé
  par M1). Accountability prise, pas de transfert de blâme.
- `docs/governance/daily/M2-2026-04-22.md` §Preuves techniques et
  §Risques : mêmes corrections, R-M2-01 réduit au rang d'hygiène.

## Preuves techniques

### Sanity : AST parse après fix

```
$ cd backend && python3 -c "import ast; ast.parse(open('pli/adapters/local.py').read()); print('AST OK')"
AST OK
$ wc -l pli/adapters/local.py
226 pli/adapters/local.py
$ grep -n "^class \|^async def local_principal_resolver\|plan=" pli/adapters/local.py
41:class LocalStorageAdapter(StorageAdapter):
108:class LocalSearchAdapter(SearchAdapter):
141:async def local_principal_resolver(request) -> Principal:
159:        plan="free",
169:class LocalSettingsStore(SettingsStore):
204:class LocalCryptoAdapter(CryptoAdapter):
```

### pytest mode (b) — `PLI_ENABLE_M2=1`

```
$ cd backend
$ PYTHONPATH=. PLI_ENABLE_M2=1 python3 -m pytest \
    tests/test_accounts.py tests/test_messages.py \
    tests/test_contacts.py tests/test_conversations.py \
    tests/test_auth_oauth.py \
    -p no:cacheprovider --no-cov -q
..................................................                       [100%]
50 passed in 2.34s
```

### pytest mode (a) — `PLI_ENABLE_M2` unset (non-régression)

```
$ PYTHONPATH=. python3 -m pytest <mêmes suites> -p no:cacheprovider --no-cov -q
..................................................                       [100%]
50 passed in 2.00s
```

### Critères d'acceptation ticket `M1-regression-m2.md`

| # | Critère | Statut |
|---|---------|--------|
| 1 | `PLI_ENABLE_M2=1 pytest test_{accounts,messages,contacts,conversations,auth_oauth}` → 50/50 | ✅ |
| 2 | Défaut OFF (Sprint 1) reste stable | ✅ (50/50 mode a) |
| 3 | Pas de nouvelle dép. Postgres au boot mode local | ✅ (extra `[m2]` opt-in) |
| 4 | Tag `rebaseline-base-2026-04-22` re-postable | ✅ — notifié à M1 pour T1.7 |

## Dépendances aval débloquées

- **M1** : T1.7 peut re-poser le tag `rebaseline-base-2026-04-22`. Ticket
  `M1-regression-m2.md` fermable côté journal M1.
- **M3** : baseline re-mesurable, `PLI_ENABLE_M2=1` effectif et Sprint 1
  intact en mode (b).
- **M4** : idem pour re-baseline bêta / OCR / GDPR.
- **M5** : freeze `ADR 0008` inchangé ; le fix respecte le contrat.

## Risques / alertes direction

1. **R-M2-05 — robustesse édition OneDrive** : pendant l'édition de
   `pli/adapters/local.py`, OneDrive a tronqué temporairement le
   fichier (lignes ~191+ disparues côté sandbox alors que le fichier
   OneDrive restait complet). Reconstruction via script Python
   idempotent. **Recommandation** : toute édition de fichier Python
   critique sur le repo doit être suivie d'un `python3 -c 'import ast;
   ast.parse(open(f).read())'` et d'un `wc -l` cross-checkés
   sandbox/OneDrive avant commit. À formaliser en checklist PR par M5.
2. **R-M2-06 — dette Sprint 2/3 licence locale** : le hardcode
   `plan="free"` doit être retiré dans le refactor Sprint 2/3 (branche
   sur `pli.licensing.local_key.load_license().plan`). Ticket de
   suivi : créer un `tickets/S2-licence-locale-plan-resolver.md` en
   ouverture Sprint 2 pour que la dette ne se perde pas. Pas de risque
   si l'utilisateur n'a pas encore acheté PLI Plus (M1 est l'unique
   tenant free).

## En cours demain (2026-04-24)

- Attendre confirmation M1 sur pose tag `rebaseline-base-2026-04-22`
  pour clore formellement la boucle.
- Reprendre T2.3 #42 (mention rétractation 14 j Checkout).
- Suivi relance avocat #40 / DPO #41 (pas avant 2026-04-27, 2 jours
  ouvrés après demande initiale 2026-04-22).

## Métriques

- Hotfix P0 livré dans les 24 h (ouverture M1 2026-04-22 → fix M2
  2026-04-23), sous la deadline direction.
- 50/50 tests en mode (b) et (a).
- 0 commit direct sur `main` (règle §2 D3).
- 1 correction diagnostic posée publiquement (accountability).

---

_Rédigé par session M2, 2026-04-23. Format aligné sur
`docs/governance/daily/M1-2026-04-22.md` et
`docs/governance/daily/M2-2026-04-22.md`._
