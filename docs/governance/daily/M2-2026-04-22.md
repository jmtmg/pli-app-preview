# Daily M2 — 2026-04-22

**Session** : M2 (auth PLI + billing Stripe + licensing + tenancy)
**Format** : log daily — ordre de mission 2026-04-22 §4
**Statut sprint** : Sprint 6 closé → P0 unblock exécuté — ticket
`tickets/M2-unblock-auth.md`

---

## Ack

- Ordre de mission `2026-04-22-ordre-mission.md` lu et appliqué.
- Ticket `tickets/M2-unblock-auth.md` récupéré (P0 bloquant M3/M4/M5).
- Checkpoint 38/42 revu — 4 items restants identifiés (cf. Journal
  ticket, section « Checkpoint 38/42 → 42/42 »).
- Aucun nouveau scope engagé (règle convergence §4 respectée).

## Réalisé aujourd'hui

- **T2.1** ✅ Option A retenue : `pli/auth_pli/` → `pli/auth/` par `mv`
  + patch imports. Ajout des deux modules manquants que les
  consommateurs M3/M4 attendaient (`pli/auth/dependencies.py` avec
  `current_user` / `require_admin`, `pli/auth/deps.py` avec la
  dataclass `User`). Condition Done respectée : plus aucune erreur
  de collection pytest n'est attribuable à `pli.auth`.
- **T2.2** ✅ Wire-up conditionnel `PLI_ENABLE_M2` dans `pli/main.py` :
  flag déclaré dans `pli/config.py` via `pydantic-settings` (pas un
  `os.getenv` ad-hoc — cohérence avec le reste de la config), bloc
  `if settings.enable_m2:` qui monte `auth_pli`, `billing`, `licenses`
  + `TenantContextMiddleware`. Défaut `False` ⇒ Sprint 1 / M1 intact.
  Log `pli_m2_routers_enabled` / `disabled` au démarrage pour trace
  opérationnelle.
- **ADR 0007** ✅ Créé : `docs/adr/0007-feature-flag-m2.md` (99 lignes).
  Décision + alternatives (no-flag, branche long-lived, per-router)
  + plan de rollout 2026-04-30 → 2026-08-26 + critères de suppression
  (2 sprints sans régression + couverture ≥ 80 % + validation
  webhooks Stripe + signature direction) + rollback
  (`fly secrets set PLI_ENABLE_M2=0`).
- **Journal ticket** ✅ Complété avec résumé Option A, preuves pytest
  (avant/après), statut 4 conditions checkpoint avec ETA.
- **T2.3** 🟡 Statut préparé (cf. Journal ticket) :
  - #39 Coverage ≥ 80 % — en cours, dépend CI 3.11. ETA 2026-05-05.
  - #40 CGU v2 — WAITING_HUMAN avocat. Draft posé. ETA 2026-05-10.
  - #41 Privacy policy v2 — WAITING_HUMAN avocat/DPO. Draft posé.
    ETA 2026-05-10.
  - #42 Mention rétractation 14 j Checkout — prêt à merger avec PR
    T2.2. ETA 2026-04-30.

## Preuves techniques

Cache Python vidé avant chaque run (OneDrive garde des `.pyc` stale,
`__pycache__` dégagés à la main).

### `pytest --collect-only tests/`

```
$ PYTHONDONTWRITEBYTECODE=1 pytest --collect-only -p no:cacheprovider \
    --no-cov tests/
...
==================== 144 tests collected, 5 errors in 0.87s ====================
```

Les 5 erreurs résiduelles ne proviennent **plus** de `pli.auth` (zéro
occurrence de `ModuleNotFoundError: No module named 'pli.auth'`). Elles
pointent toutes sur des imports M3/M4 :

- `test_beta_*` + `test_gdpr` → `pli.db.models` / `get_session` absents
  (scope M4 — `pli/db.py` est un module, pas un package avec
  sous-modules SQLAlchemy).
- `tests/unit/ocr/test_worker.py` → `pli.db.session` (scope M3).

Remontées côté M3 et M4 via leur daily respectif.

### `PLI_ENABLE_M2=1 pytest`

```
$ PLI_ENABLE_M2=1 pytest -p no:cacheprovider --no-cov \
    --ignore=tests/test_beta_* --ignore=tests/test_gdpr.py \
    --ignore=tests/unit/ocr/test_worker.py tests/
...
42 passed, 3 skipped, 99 errors in 1.11s
```

**Passent (cœur M2 vivant)** :

- `test_licensing.py` 6/6 — signature Ed25519 + rejet tampering.
- `test_stripe_webhooks.py` 8/8 — plan computation + idempotence.
- `test_emails.py` 5/5 — verification / reset / trial / invoice.
- `test_tenancy_isolation::test_repository_refuses_sql_without_user_id`.
- `test_db_schema.py` 13/13.
- `test_parser.py` 5/5 — normalize / classify / snippet.
- `test_sync_gmail.py` 4/4 — extraction raw Gmail.

**3 skips** : tests storage cloud S3 (pas de boto3/MinIO en sandbox, ok).

**Les 99 errors** sont tous des `fixture 'client' not found`.

> **Correction 2026-04-23** : le diagnostic initial (« régression `pytest
> 9.0.3` × `pytest-asyncio 1.3.0` sur Python 3.10 sandbox ») était **faux**.
> M1 a identifié la cause réelle pendant T1.6 : `tests/conftest.py` était
> tronqué à **49 lignes** sur disque (coupé en plein commentaire de
> `isolated_settings`, sans la fixture `client`). Reconstruction par M1 à
> **71 lignes** (fixture `client` + shim `datetime.UTC`). Cf. daily
> `M1-2026-04-22.md` §T1.6 et journal `M2-unblock-auth.md`. Je n'avais pas
> vérifié l'état réel du fichier sur disque avant de conclure à un problème
> d'env — accountability prise côté M2, pas de blâme transféré à M5.
> L'épingle `pytest` / `pytest-asyncio` reste une hygiène utile (R-M2-01)
> mais n'est pas la cause racine des 99 erreurs.

## Blocage / WAITING_HUMAN

- **T2.3 #40 CGU v2** : drafté, en attente retour avocat externe.
- **T2.3 #41 Privacy policy** : drafté, en attente avocat + DPO.
- **T2.3 #39 Coverage 80 %** : bloqué côté sandbox Python 3.10 ;
  débloque automatiquement côté CI 3.11 dès que la PR T2.2 est mergée.

## Dépendances aval débloquées

- **M3** peut démarrer re-baseline : `run-lighthouse-integrated.sh`
  exige `PLI_ENABLE_M2=1`, désormais fonctionnel.
- **M4** peut démarrer re-baseline bêta / GDPR.
- **M5** peut préparer freeze Launch sur binaire intégré.

Coordination posée en session de sync. Aucune PR directe vers `main`
(règle ordre de mission §2 D3 respectée) ; la livraison passera par
une PR review-gatée dès que direction confirme.

## En cours demain (2026-04-23)

- Clôture #42 (mention rétractation 14 j) : dernière relecture copy +
  snapshot test visuel.
- Suivi CI 3.11 (GitHub Actions) pour lever #39 dès PR T2.2 mergée.
- Relance avocat et DPO sur #40 et #41 (pas avant 2 jours ouvrés).
- Pas de commit vers `main` tant que direction n'a pas validé la
  bascule PR → merge.

## Risques / alertes direction

1. **R-M2-01 — env sandbox 3.10 vs CI 3.11** (révisé 2026-04-23) :
   divergence sandbox/CI sur Python 3.10 vs 3.11. **Ne plus présenter
   comme cause des 99 errors fixture `client`** (cf. correction preuves
   ci-dessus). Reste une recommandation d'hygiène : épingler `pytest` et
   `pytest-asyncio` en exact pin (`==`) dans
   `[project.optional-dependencies] dev`, pour que tout upgrade passe par
   un ADR. Priorité abaissée : hygiène, pas P0.
2. **R-M2-02 — déblocage M3/M4 chrono** : si la PR T2.2 n'est pas
   mergée d'ici **2026-05-07**, M3 rate sa fenêtre de re-baseline
   (2026-05-14) et le Launch J0 2026-10-07 glisse. Le code est prêt,
   le goulot devient uniquement la review.
3. **R-M2-03 — retours juridiques #40/#41** : si le cabinet ne rend pas
   la v2 CGU/privacy avant 2026-05-10, le checkpoint 42/42 ne peut
   pas se fermer dans les temps même avec tout le technique livré. À
   arbitrer par direction : retenir la version courante avec mentions
   explicites « v2 en cours de relecture » ou décaler T2.3 ?
4. **R-M2-04 — couche OCR M3 qui casse côté test_worker** : l'import
   `pli.db.session` n'existe pas parce que `pli/db.py` est un fichier
   simple, pas un package. M3 devra soit migrer `pli/db.py` → package
   `pli/db/__init__.py` + `session.py`, soit corriger l'import côté
   `pli/ocr/api.py`. Remonté à M3 dans leur daily — pas une décision
   M2.

## Questions à direction

1. **PR T2.2** : review par qui et sous quel SLA ? Deux reviewers
   minimum (un backend, un sécu/billing) ou une review unique accélérée
   vu le caractère P0 ?
2. **CGU / privacy v2** : si avocat n'a pas rendu au 2026-05-10,
   clôture checkpoint sur v1.5 avec mention « v2 en file » ou bloquer
   T2.3 ? Préférence direction ?
3. **Feature flag post-Launch** : ADR 0007 prévoit suppression du flag
   2 sprints sans régression après Launch (≈ 2026-08-26). Direction
   souhaite-t-elle plutôt un retrait plus tardif (après un trimestre
   de prod complet) ?

## Métriques

- 3/3 tâches P0 livrables aujourd'hui DONE (T2.1, T2.2, ADR 0007).
- T2.3 : 1/4 prêt à livrer (#42), 1/4 en cours côté M2 (#39), 2/4
  WAITING_HUMAN (#40, #41).
- 0 régression M1 (flag off par défaut, vérifié `settings.enable_m2
  == False`).
- 0 commit sur `main` (règle §2 D3).
- Tests cœur M2 verts : 42 passed / 3 skipped / 0 failure côté
  logique métier.

---

_Rédigé par session M2, 2026-04-22. Format aligné sur
`docs/governance/daily/M1-2026-04-22.md` et
`docs/governance/daily/M3-2026-04-22.md`._
