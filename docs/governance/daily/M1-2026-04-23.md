# Daily M1 — 2026-04-23

**Session** : M1 (mail conversationnel)
**Format** : log daily — ordre de mission 2026-04-22 §4
**Statut sprint** : Sprint 1 closeout — re-vérification post-hotfix M2

---

## Réalisé aujourd'hui

- **T1.6 re-vérif** ✅ CLOSED — Fix M2 mergé (Fix C, plan="free"), extra
  `[project.optional-dependencies].m2` ajouté dans `pyproject.toml`.
  Relu `pli/adapters/local.py` et `pyproject.toml` côté code, puis
  ré-exécution des deux modes :
  - Mode (a) `PLI_ENABLE_M2` unset : 17 paths openapi (0 M2),
    `pli_m2_routers_disabled`, **50/50 pytest verts en 1.62s**.
  - Mode (b) `PLI_ENABLE_M2=1` : 31 paths openapi (+14 M2 : `/auth/login`,
    `/auth/signup`, `/billing/billing/*`, `/licenses/licenses/*`),
    `pli_m2_routers_enabled`, **50/50 pytest verts en 1.88s**.
  - Critères #1-4 du ticket `M1-regression-m2.md` validés.
  - Régression-ticket à marquer RÉSOLU dans son Journal.
- **T1.7** 🟡 DÉLÉGUÉ direction Mail — la sandbox M1 n'est pas un git
  repo (`fatal: not a git repository`). Direction Mail (session pilote)
  pose le tag `rebaseline-base-2026-04-22` depuis sa sandbox puis
  notifie M3 + M4 + M5. SHA à consigner ici dès posé.

## Bloqué / WAITING_HUMAN

- **T1.2** : OAuth Gmail réel (Google Cloud Console + écran de
  consentement + test user). Indépendant du fix M2. Runbook prêt
  (`docs/runbooks/oauth-gmail-setup.md`).
- **T1.3** : capture des 4 screenshots E2E. Dépend T1.2.

## En cours demain (2026-04-24)

- Stand-by jusqu'à ce que (1) opérateur humain remonte les retours
  T1.2/T1.3 et (2) direction Mail confirme le tag posé.
- Premier daily M1 régulier (post-closeout) une fois T1.7 confirmé.

## Risques / alertes direction

1. **Smoke-test side-effect (non-bloquant)** : `PLI_ENABLE_M2=''`
   (variable définie à chaîne vide) déclenche une `ValidationError`
   pydantic (`Input should be a valid boolean`). Le défaut Sprint 1
   est `unset`, qui marche bien — mais une CI naïve qui ferait
   `export PLI_ENABLE_M2=` crasherait au boot. Recommandation : noter
   dans le runbook ops "ne pas définir la var à vide ; pour désactiver,
   l'omettre / `unset`".
2. **Pattern troncature OneDrive** : confirmé en T1.6 sur conftest.py
   (49 → 71 lignes), et M2 a vu le même symptôme sur `pli/adapters/local.py`
   (191/227 lignes, SyntaxError). À formaliser en checklist PR par M5
   (post-edit `python3 -c 'import ast; ast.parse(open(f).read())'` +
   `wc -l` cross-checké sandbox/OneDrive sur tout fichier critique).
3. **Pas d'autres régressions détectées** sur Sprint 1.

## Métriques

- 1/1 tâche re-vérif close (T1.6)
- 1/1 tâche déléguée propre (T1.7 → Mail)
- 100/100 tests Sprint 1 passants (50 mode a + 50 mode b)
- 0 régression introduite par M1 ce jour
- 0 commit dans `main` (règle ordre de mission §2 D3 respectée)

---

_Format aligné sur `docs/governance/daily/M1-2026-04-22.md`._
