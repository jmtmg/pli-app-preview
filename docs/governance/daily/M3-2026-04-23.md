# Daily M3 — 2026-04-23

**Session** : M3 (Bêta / OCR / sécu)
**Status** : actif — §7 baseline validé, démarrage re-baseline sécurité partiellement bloqué par infra manquante
**Baseline de mesure** : `rebaseline-base-2026-04-22` — empreinte globale `6a8d82bcbd292422f3ef87d0d6a30dce`
**Prochaine mise à jour** : 2026-04-24

---

## Ack

- Baseline `rebaseline-base-2026-04-22` reçue de direction (manifeste md5 dans `docs/governance/tags/`).
- Greenlight re-baseline sécurité confirmé.
- Règle §7 appliquée avant toute campagne : reconstituer manifestes locaux, diff contre référence, suspendre si un seul hash diffère.
- Blocage amont M2 T2.2 levé (daily 2026-04-22 arbitrage direction confirme M1 T1.6 CLOSED, mode b 17→31 paths OpenAPI attesté).

## §7 — Vérification baseline (obligatoire avant campagne)

Procédure jouée dans la sandbox Linux (Python 3.10.12) :

```
md5sum backend/pli/**/*.py (72 fichiers)     → 72/72 hashes identiques
md5sum backend/tests/**/*.py (24 fichiers)   → 24/24 hashes identiques
md5sum backend/pyproject.toml                → 1/1 identique
md5sum docs/adr/*.md (8 fichiers)            → 8/8 identiques
md5(concat(backend_md5.txt, adr_md5.txt))    → 6a8d82bcbd292422f3ef87d0d6a30dce  ✅
```

Empreinte globale locale = empreinte globale manifeste. Baseline valide.

Pytest 50/50 vérifié dans les deux modes :

```
mode (a) PLI_ENABLE_M2 unset  →  50 passed in 2.92s   ✅
mode (b) PLI_ENABLE_M2=1      →  50 passed in 3.86s   ✅
```

Réserve : la vérification `OpenAPI 17 paths (mode a) → 31 paths (mode b)` n'a pas pu être rejouée dans la sandbox (Python 3.10 vs requires-python ≥3.11 pour `datetime.UTC` dans `pli/api/accounts.py`). Les pytest passant 50/50 dans les deux modes sont un signal fort que le wire-up fonctionne — je prends le relais des attestations M1 T1.6 sur ce point plutôt que de dupliquer la mesure.

## Fait aujourd'hui

### Adaptation des runners (task M3-#24)

Workspace OneDrive non git-init → les deux runners utilisaient `git describe --exact-match --tags HEAD` comme garde-fou. Inopérant en l'état. Remplacé par appel à un nouvel helper :

- `scripts/rebaseline/check-baseline-md5.sh` (nouveau) : reconstitue les manifestes locaux, diff contre `docs/governance/tags/rebaseline-base-2026-04-22.md`, vérifie l'empreinte globale, exit 1 si drift. Override `PLI_REBASELINE_ALLOW_UNTAGGED=1` préservé pour dry-run.
- `e2e/perf/run-lighthouse-integrated.sh` : appel helper en amont, message d'abort contextualisé.
- `scripts/rebaseline/run-vuln-scans.sh` : idem.
- `docs/governance/metrics/REBASELINE-CHECKLIST.md` : étapes 2/3/4 réécrites pour refléter le substitut md5, règle d'or §finale mise à jour.

Smoke tests :

- Cas nominal : baseline vérifiée, runner démarre (bloqué ensuite par absence des outils de scan dans la sandbox — attendu).
- Cas drift simulé (hash modifié localement) : abort clair avec diff et rappel de la procédure P0-rebaseline-drift.
- Override dry-run : accepté avec warning "ne pas publier de rapport officiel".

### Tentative re-baseline sécurité

Greenlit reçu. §7 OK. Essayé de lancer `run-vuln-scans.sh` en conditions nominales — bloqué par l'infrastructure :

| Outil | Requis par script | Présent dans sandbox | Artefact requis | Status |
|-------|-------------------|----------------------|-----------------|--------|
| `pip-audit` | `--requirement backend/requirements.lock` | installé | `backend/requirements.lock` **absent** | ❌ |
| `npm audit --production` | `cd frontend && npm audit` | `npm` présent | `frontend/package-lock.json` **absent** | ❌ |
| `trivy image --severity CRITICAL,HIGH` | `pli/backend:prod` | `trivy` absent, `docker` absent | image non buildée | ❌ |

Décision : **pas de scan officiel publié ce jour.** Un scan partiel produirait des chiffres non-comparables (env sandbox Python 3.10 vs target Python 3.11+, deps non-pinnées, image inconnue) et casserait la promesse "re-baseline reproductible sur binaire intégré".

Ne PAS produire de chiffres provisoires. Meilleure voie : résoudre les prérequis infra, puis rejouer le scan officiel d'un coup.

## Blocage : prérequis infra pour scan officiel

Pour que `run-vuln-scans.sh` soit exécutable en conditions officielles, il manque trois artefacts qui n'existent pas dans le repo aujourd'hui :

1. **`backend/requirements.lock`** — actuellement pyproject.toml seulement. Il faut soit `pip-compile` depuis pyproject.toml (outil `pip-tools`), soit adopter un autre gestionnaire (uv pip compile, Poetry lock). Decision d'outillage à prendre avec M2 car impacte le pipeline CI.
2. **`frontend/package-lock.json`** — actuellement package.json seulement. Généré par `npm install` mais pas commité. Idem, decision d'outillage CI.
3. **Image `pli/backend:prod`** — `Dockerfile` présent dans `backend/` mais pas de `Dockerfile.prod` visible, et l'image n'est pas buildée. Besoin d'un runner avec Docker (la sandbox n'en a pas), et éventuellement d'un `Dockerfile.prod` distinct du dev si l'objectif est de ne pas scanner Tesseract + outils debug.

Ces trois points ne sont **pas** des drifts baseline (pyproject.toml est bien stable, md5 OK), ce sont des **prérequis de publication** manquants qui n'étaient pas visibles tant qu'on n'avait pas lancé la chaîne complète.

## Prévu demain (2026-04-24)

- Attente arbitrage direction sur les 3 prérequis infra (cf. questions ci-dessous).
- Préparer la section 3.3 (vulns) du rapport `2026-04-22-rebaseline-m3.md` avec la méthode envisagée (pip-audit + npm audit + trivy) — les cases "avant/après" restent vides jusqu'à scan officiel.
- Relire le `security/threat-model-checklist.md` : pointer les 27 contrôles qui supposaient un binaire intégré (candidats re-test en même temps que T3.2).
- Préparer fixtures OCR `backend/tests/fixtures/ocr/invoice_scanned_3pages.pdf` (référencée par `e2e/ocr/integrated.spec.ts` mais pas committée).

## Risques suivis (mise à jour)

- **R-M3-01** (runway) : sans blocage infra, runway 22 jours tenable. Avec blocage infra, chaque jour de délai sur les 3 prérequis mange directement sur mon calendrier de scan+rapport. À surveiller.
- **R-M3-02** (régression chiffres) : inchangé.
- **R-M3-03** (trivy --ignore-unfixed) : inchangé.
- **R-M3-04 NOUVEAU** : la sandbox Python 3.10 masque la vérification OpenAPI 17→31. Si quelqu'un doit rejouer §7 dans un env similaire, la checklist doit être clarifiée. Proposition : `check-baseline-md5.sh` vérifie md5 + pytest 50/50, et la vérification OpenAPI reste à la charge de l'env de mesure officiel (Python ≥3.11).

## Questions à direction

1. **Outillage lock Python** — on génère `backend/requirements.lock` via `pip-tools` (`pip-compile pyproject.toml`) ou `uv pip compile` ? La décision est structurante car le lock devra ensuite être committé, régénéré à chaque bump de dépendance, et surveillé par CI. Préférence M3 : `pip-compile` (outil le plus classique, bonne doc, pas de dépendance à un runtime tiers), mais l'arbitrage revient à M2 + direction.
2. **Outillage lock Node** — même question côté frontend : on commit `package-lock.json` généré par `npm install` ou on passe à `pnpm`/`yarn` ? Préférence M3 : `package-lock.json` + `npm ci` en CI (alignement avec `npm audit --production` déjà dans le script).
3. **Image scan trivy** — `Dockerfile` actuel contient probablement les outils de build / Tesseract en dev. Pour scanner en prod, il faut soit un `Dockerfile.prod` séparé (multi-stage final), soit un `--target prod` si le Dockerfile est déjà multi-stage. M4 a-t-il déjà une préférence sur ce point (impact sur son build de perf aussi) ?
4. **Qui builde l'image + qui restaure la seed** — pour la séquence §Étape 2 de REBASELINE-CHECKLIST.md (ex-étape "direction build l'image Docker prod depuis le tag"). Direction avait confirmé le 2026-04-22 que le build venait de direction. Je confirme juste que ça tient avec les nouveaux prérequis, ou direction délègue à M2 qui a la clé SSH déploiement ?

## Actions post-daily (à valider demain)

- Si arbitrages reçus avant end-of-day 2026-04-24 : lancement scan officiel 2026-04-25 au plus tôt.
- Si pas d'arbitrages : rappel escalade dans le daily du 2026-04-25.

---

_Rédigé par session M3, 2026-04-23._
